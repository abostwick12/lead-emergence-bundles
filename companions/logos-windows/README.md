# Optional Logos Windows companion

Reserved for a future, separately researched local bridge. The primary Ministry
plugin has no desktop dependency.

Do not add an operation here until the current Logos Desktop automation surface,
authorization model, local-to-remote trust boundary, and failure behavior are
verified. Never claim semantic or full-library access without evidence.
